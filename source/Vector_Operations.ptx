<section xml:id="Vector_Operations">
  <title>Vector Operations</title>
  <definition>
    <statement>
      <p>
        Let <m>\vect{v}=\ot{v_1}{v_2}{v_3}</m> and <m>t \in \mathbb{R}</m>.
        The <term> scalar product</term> of <m>t</m> and <m>\vect{v}</m> is defined by:
        <m>t\vect{v} = \ot{tv_1}{tv_2}{tv_3}</m>
      </p>
    </statement>
  </definition>
  <definition>
    <statement>
      <p>
        Let <m>\vect{v}=\ot{v_1}{v_2}{v_3}</m> and <m>\vect{w}=\ot{w_1}{w_2}{w_3}</m>.
        The <term>sum</term> of <m>\vect{v}</m> and <m>\vect{w}</m> is defined by:
        <m>\vect{v}+\vect{w} =\ot{v_1+w_1}{v_2+w_2}{v_3+w_3}</m>.
      </p>
    </statement>
  </definition>
  <p>
    These are algebraic definition.
    It is also important to understand how these definitions affect vectors geometrically.
  </p>
  <exercise>
    <statement>
      <p>
        Use the vector <m>\vect{v}</m> below and give geometric representations of the vectors described by the scalar products:
        <m>3\vect{v}</m> and <m>-1\vect{v}</m>. Use similar triangles to justify your answers.
      </p>
		<image source="vector" width="40%"><description> Image of an arrow. </description></image>
    </statement>
  </exercise>
  <exercise>
    <statement>
      <p>
        Given <m>\vect{v} = \ot{1}{-2}{2}</m>,
        find a vector with the same direction as
        <m>\vect{v}</m>, but with length <m>1</m>,
        with length <m>10</m>, and with length <m>h</m>.
      </p>
    </statement>
  </exercise>  
  <definition>   
	<statement>
	  <p>
		Let <m>\vect{v} \in \mathbb{R}^3</m>. The <term>direction</term> of <m>\vect{v}</m> is
		the vector <m>\vect{u}</m> such that <m>\vect{v}= \|\vect{v}\|\vect{u}</m>.
		This makes any vector equal to the scalar product of its length and direction. 
	  </p>
	</statement>
  </definition>   
  <exercise>
    <statement>
      <p>
        Use the vectors <m>\vect{v}</m> and <m>\vect{w}</m> below to give a geometric representation of the vector which is the sum <m>\vect{v}+\vect{w}</m>.
        If this interpretation is applied to <m>\vect{w}</m>+<m>\vect{v}</m> is the result the same?
        If this interpretation is applied to <m>(\vect{v} +\vect{w}) + \vect{u}</m> and
        <m>\vect{v} + ( \vect{w} + \vect{u})</m> are those results the same?
      </p>
		<image source="vectors" width="90%"><description> Image of three arrows. </description></image>
    </statement>
  </exercise>
  <theorem xml:id="show_exists">
    <statement>
      <p>
        For any ordered triple <m>\vect{v}=\ot{v_1}{v_2}{v_3} \in \mathbb{R}^{3}</m> there exists an ordered triple <m>\vect{x}=\ot{x_1}{x_2}{x_3} \in \mathbb{R}^{3}</m> such that when
        <m>\vect{x}</m> is added to <m>\vect{v}</m> the result is the <term>origin</term>,
        <m>\vect{0}=\ot{0}{0}{0}</m>.
        For a given <m>\vect{v}</m> such an ordered triple is unique.
        Geometrically the vector <m>\vect{x}</m> can be visualized as <fillin/>. (Describe starting and ending positions,
        or direction and length.)
      </p>
 
      <p>
        Also, for any ordered triples <m>\vect{v}=\ot{v_1}{v_2}{v_3} \in \mathbb{R}^{3}</m> and
        <m>\vect{w}=\ot{w_1}{w_2}{w_3} \in \mathbb{R}^{3}</m> there exists an ordered triple <m>\vect{y}=\ot{y_1}{y_2}{y_3} \in \mathbb{R}^{3}</m> such that when
        <m>\vect{y}</m> is added to <m>\vect{v}</m> the result is <m>\vect{w}=\ot{w_1}{w_2}{w_3}</m>.
        For any given <m>\vect{v}</m> and
        <m>\vect{w}</m> such an ordered triple is unique.
        Geometrically the vector <m>\vect{y}</m> can be visualized as <fillin/>. (Describe starting and ending positions,
        or direction and length.)
      </p>
    </statement>
  </theorem>
  <p>
    <xref ref="show_exists" /> provides justification for the following definitions.
  </p>
  <definition>
    <statement>
      <p>
        Let <m>\vect{v}=\ot{v_1}{v_2}{v_3} \in \mathbb{R}^3</m>.
        The <term>additive inverse</term>
        of <m>\vect{v}</m> is the ordered triple, written <m>-\vect{v}</m>,
        that, when added to <m>\vect{v}</m> results in the origin:
        <m>\vect{0} = \ot{0}{0}{0}</m>.
        In symbols <m>-\vect{v}</m> is the ordered triple such that <m>\vect{v} + -\vect{v}=0</m>.
      </p>
    </statement>
  </definition>
  <definition>
    <statement>
      <p>
        Let <m>\vect{v}=\ot{v_1}{v_2}{v_3} \in \mathbb{R}^3</m> and <m>\vect{w}=\ot{w_1}{w_2}{w_3} \in \mathbb{R}^3</m>.
        The <term>difference</term> of <m>\vect{w}</m> from <m>\vect{v}</m> is the ordered triple,
        written <m>\vect{w}-\vect{v}</m>,
        which when added to <m>\vect{v}</m> results in <m>\vect{w}</m>. In symbols
        <m>\vect{w}-\vect{v}</m> is the ordered triple such that <m>\vect{v} +(\vect{w}-\vect{v})=\vect{w}</m>.
      </p> 
    </statement>
  </definition>
  <question>
    <p>
      Do <m>\vect{w}-\vect{v}</m> and
      <m>\vect{v}-\vect{w}</m> describe the same ordered triple?
	  Do <m>\vect{w}-\vect{v}</m> and
      <m>\vect{w} + \vect{-v}</m> describe the same ordered triple?
    </p>
  </question>
  <proposition>
	<p>
		Let <m>\vect{v}=\ot{v_1}{v_2}{v_3} \in \mathbb{R}^3</m> and <m>k \in \mathbb{R}</m>.
		Does <m>\|k\vect{v}\| = k\|\vect{v}\|</m>?
		Does <m>\| \vect{v} + \vect{w}\| =\|\vect{v}\|+\|\vect{w}\|</m>?
		Include justification.
	</p>
  </proposition>


  
</section>